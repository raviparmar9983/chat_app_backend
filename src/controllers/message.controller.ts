import { NextFunction, Request, Response } from "express";


const sendMessage = async (req: Request, res: Response, next: NextFunction) => {
    try {

    }
    catch (err) {
        next(err);
    }
}


const getMessges = async (req: Request, res: Response, next: NextFunction) => {
    try {

    }
    catch (err) {
        next(err);
    }
}
